apiVersion: v1
kind: Pod
metadata:
  name: with-node-affinity
spec:
  affinity:
    nodeAffinity:
      # requiredDuringSchedulingIgnoredDuringExecution:
      #   nodeSelectorTerms:
      #   - matchExpressions:
      #     - key: hardware
      #       operator: In
      #       values:
      #       # - gpu 
      #       - gpuu
      preferredDuringSchedulingIgnoredDuringExecution: 
      - weight: 1
        preference:
          matchExpressions:
      #     - key: another-node-label-key 
            - key: hardware
              operator: In # matching labels
              values:
      #       - another-node-label-value
              - gpu

     
  containers:
  - name: with-node-affinity
    image: nginx